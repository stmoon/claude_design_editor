# Claude Design Editor 개발

## CAUTION
- 클로드가 이 파일을 임의로 변경할 수 없다.

## Requirements
- 크롬 웹브라우저 환경에서 동작한다.
- 전체 화면
  - 전체적으로 파워포인트 처럼, 왼쪽에는 탭이 있고 슬라이드 장을 선택할 수 있다.
  - 오른쪽에는 슬라이드를 표시한다.
  - panel 사이 width는 마우스로 조절할 수 있어야 한다.
- layout 설정 기능
  - 각 장은 layout을 선택할 수 있어야 한다.
  - layout 설정 시 panel 간의 사이즈를 좌우, 상하에 대해 마우스로 조절할 수 있어야 한다.
  - 아래와 같은 layout 종류가 있어야 한다.  
      - 글 위, 그림 (혹은 테이블) 아래
      - 글 왼쪽, 그림 (혹은 테이블) 오른쪽
      - 글 위, 그림 (혹은 테이블) 왼쪽
      - 글 아래, 그림 (혹은 테이블) 위
      - 글1 왼쪽 상단 , 그림1 (혹은 테이블) 왼쪽 하단, 글2 오른쪽 상단 , 그림2 (혹은 테이블) 오른쪽 하단
      - layout을 직관적으로 볼 수 있도록 그림으로 표현해준다.
  - 그림과 글이 좌우로 있는 경우 윗쪽으로 정렬되어야 한다.
  - 그림의 경우 좌우상하로 그림을 추가할 수 있는 기능이 있어야 한다.
  - 그림이 여러개인 경우 오와 열이 맞아야 한다.
- 저장 기능
  - 저장하면 열려져있는 html 에 바로 저장한다.
  - 단축키는 ctrl + s 이다
  - "다름이름으로 저장" 을 누르면 다른 이름으로 저장한다.
- 변경된 부분이 있으면 자동으로 업데이트 되어야 한다.
- 글을 변경할 수 있어야 한다.
- 특정 항목을 삭제할 수 있어야 한다. 
  - 단축키로 del 키 사용한다.
- 진행했던 일을 ctrl + z로 되돌릴 수 있어야 한다.
